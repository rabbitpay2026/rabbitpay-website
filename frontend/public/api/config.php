<?php
/**
 * RabbitPay email settings.
 *
 * IMPORTANT: replace the placeholder below with your real Resend API key.
 * Edit this AFTER uploading, directly in Hostinger's File Manager, so your key
 * is never committed to git. PHP files run on the server and are never shown to
 * visitors, so the key stays private (the .htaccess also blocks direct access).
 */

define('RESEND_API_KEY', 're_PASTE_YOUR_KEY_HERE');
define('EMAIL_TO', 'avijeetdey.email@gmail.com');
define('EMAIL_FROM', 'RabbitPay <onboarding@resend.dev>');
