# RabbitPay — Hostinger (Apache) config

RewriteEngine On

# Route the demo-form POST (/api/leads) to the PHP handler.
RewriteRule ^api/leads/?$ api/leads.php [L]

# Never serve the email config as a downloadable file.
<Files "config.php">
  Require all denied
</Files>
