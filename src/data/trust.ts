/** Trust indicators under the "Powered by COD King" badge. */
export const TRUST_POINTS = [
  "Trusted by Indian Shopify Merchants",
  "Built for High-COD Businesses",
  "Lower Return-to-Origin Rates",
  "Proven Checkout Infrastructure",
];

/** Bullet list in the support intro. */
export const SUPPORT_PROMISES = [
  "Fast responses during business hours",
  "Ops and tech on the same channel",
  "Launch help when you need a faster rollout",
];

/**
 * Hero trust chips.
 *
 * The setup-time chip reads "about an hour" rather than "minutes" so it matches
 * the one setup figure the rest of the site states — the `setup-time` FAQ ("live
 * on your Shopify store within 1 hour") and the same sentence on
 * /what-is-rabbitpay and in llms.txt. The OG image renders this same list.
 */
export const HERO_BADGES = ["Zero setup fee", "Live in about an hour", "Made in India"];
