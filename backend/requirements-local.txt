# Minimal dependencies to RUN the API locally (server.py only).
# The full requirements.txt includes extras (emergentintegrations, tooling) that
# are only available inside the Emergent platform and aren't needed to run the app.
fastapi
uvicorn
motor
pymongo
pydantic[email]
python-dotenv
httpx
